import { View, Text } from "react-native";
import React from "react";

const VideoPlayerView = () => {
  return (
    <View className="flex-1 bg-blue-50">
      <Text>VideoPlayerView</Text>
    </View>
  );
};

export default VideoPlayerView;
